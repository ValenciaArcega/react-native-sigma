import { StyleSheet } from "react-native";

const s = StyleSheet.create({
  ButtonSignOut: {
    width: "80%",
    height: 44,
    alignSelf: 'center',
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: "red",
  },
  ButtonSignOutText: {
    color: 'white',
    fontWeight: 600,
    fontSize: 18,
  }
});

export default s;
